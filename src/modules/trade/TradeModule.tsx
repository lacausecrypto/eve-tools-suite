import "./app/messages";
import { TradeApp } from "./app/TradeApp";

/** Point de montage du module Trade Co-Pilot dans le shell de la suite. */
export function TradeModule() {
  return <TradeApp />;
}

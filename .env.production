# Variables embarquées dans les builds de PRODUCTION (`tauri build` / `vite build`).
# Lu automatiquement par Vite en mode production. VERSIONNÉ volontairement :
# la clé « projet » PostHog (phc_) est PUBLIQUE par conception (write-only,
# ingestion seule — ne peut ni lire les données ni accéder au compte). Voir
# src/core/analytics. La collecte reste opt-in stricte et anonyme côté app.

# Clé projet PostHog (publique).
VITE_POSTHOG_KEY=phc_DdwXnRJWSMeGAQFPkEE7EFh8aRYGhNDLCvadLjkqZnTY

# Hôte d'ingestion PostHog Cloud EU (RGPD — données en UE).
VITE_POSTHOG_HOST=https://eu.i.posthog.com
